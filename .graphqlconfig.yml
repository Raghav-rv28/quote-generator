projects:
  quotegenerator:
    schemaPath: src/graphql/schema.json
    includes:
      - pattern
    excludes:
      - ./amplify/**
    extensions:
      amplify:
        codeGenTarget: typescript
        generatedFileName: src/API.ts
        docsFilePath: .
extensions:
  amplify:
    version: 3
